#ifndef UI_H
#define UI_H

// Initialize UI components
void ui_init(void);

// Clear the screen
void ui_clear(void);

// Draw the background (e.g., background image)
void ui_draw_background(void);

// Draw game elements (cards, bets, etc.)
void ui_draw_game(void);

// Refresh the display
void ui_update(void);

#endif // UI_H
